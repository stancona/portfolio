---
status: Draft | Approved
derived-from: ../intent.md
sign-off: [product owner]
date: YYYY-MM-DD
---

# Specification: [Descriptive name]

<!--
Golden template — `stancona/srd` payload ([M:sdlc], TEMPLATE.md §5.5).
Generated by the agent from the accepted intent and committed alongside it in the same work-item
folder, with skills applied (brand, security, UX standards). The product owner approves and never
writes; flagged concerns are resolved with their policy owners before engineering sees this spec.
-->

## Technical shape

How the solution fits the current architecture; components created, changed, or retired.

## Interfaces & contracts

API endpoints, data contracts, events, and schema changes — concrete, with request/response shapes.

## Edge cases & error handling

Malformed input, concurrency conflicts, failure and recovery behaviour.

## Test strategy

Unit / integration / eval coverage that will prove the plan's "Proof" section.

## Flagged concerns

Policies that could not be satisfied or contradict each other — each with its policy owner and
resolution status. Unresolved items block the approval gate.

## Sign-off

- [ ] Product owner approved — opens the Build stage (issue label `status:ready`, board `Ready`).
